name: Deploy to GitHub Pages

# DISABLED: Using Netlify for deployment instead
# To enable GitHub Pages deployment:
# 1. Go to repository Settings > Pages
# 2. Enable GitHub Pages
# 3. Set source to "GitHub Actions"
# 4. Uncomment the on: section below

on:
  workflow_dispatch:  # Manual trigger
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy-frontend:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      
      - name: Install dependencies
        working-directory: ./frontend
        run: npm ci
      
      - name: Build
        working-directory: ./frontend
        run: npm run build
        env:
          VITE_API_BASE: ${{ secrets.VITE_API_BASE || 'https://crossify-platform-production.up.railway.app/api' }}
          VITE_BASE_PATH: /
          VITE_ETH_FACTORY: ${{ secrets.VITE_ETH_FACTORY || '0x8eF1A74d477448630282EFC130ac9D17f495Bca4' }}
          VITE_BSC_FACTORY: ${{ secrets.VITE_BSC_FACTORY || '0xFF8c690B5b65905da20D8de87Cd6298c223a40B6' }}
          VITE_BASE_FACTORY: ${{ secrets.VITE_BASE_FACTORY || '0x170EE984fBcfd01599312EaA1AD4D35Ad5e66f58' }}
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './frontend/dist'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4








